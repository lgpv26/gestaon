import _ from 'lodash'

const state = () => ({
    disabled: false
})

const getters = {
}

const mutations = {
}

const actions = {
}

export default {
    namespaced: true,
    state: state(),
    getters,
    mutations,
    actions
}
